# NVIDIA API Key (Required)
# Get your free API key at: https://build.nvidia.com/
NVIDIA_API_KEY="your-nvidia-api-key-here"

# RAG Service Configuration (Optional)
CHROMA_PERSIST_DIR="./chroma_db"  # Directory for persistent vector storage
RAG_PORT=8000  # Port for Python RAG service (leave unset to auto-pick)

# Logging (Optional)
LOG_LEVEL="info"  # Options: error, warn, info, debug
